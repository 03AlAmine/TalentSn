rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ MODE TEST UNIQUEMENT - À MODIFIER AVANT PRODUCTION ⚠️
    match /{document=**} {
      allow read, write: if true;
    }
  }
}